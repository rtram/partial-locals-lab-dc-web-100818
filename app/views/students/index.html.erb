<h1>Student Index</h1>

<h4>Search By Student Name</h4>
  <%= form_tag(students_path, method: :get) do  %>
    <%= text_field_tag(:search, params[:search]) %>
    <%= submit_tag ("Search") %>
  <% end %>

  <% @students.each do |student| %>
    <ul>
      <li>
        <%= link_to student.name, student%>
        <%= render partial:"student", locals:{student:student} %>
      </li>
    </ul>
  <% end %>
